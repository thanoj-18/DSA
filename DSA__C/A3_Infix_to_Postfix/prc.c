#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>
int main()
{
    // char exp[50];
    // printf("Enter the Expression :");
    // scanf("%s",&exp);

    char exp[50] = "2+6"; // 26+

    int n = strlen(exp);

    // for(int i=0;i<n;i++){
    char ch = exp[0];
    // if()
    // }
    printf("%d", ch);

    return 0;
}